apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "elt-platform.fullname" . }}-frontend
  labels:
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "elt-platform.fullname" . }}
      app.kubernetes.io/component: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "elt-platform.fullname" . }}
        app.kubernetes.io/component: frontend
    spec:
      containers:
        - name: frontend
          image: {{ include "elt-platform.frontendImage" . }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.frontend.port }}
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: {{ .Values.frontend.port | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "elt-platform.fullname" . }}-frontend
spec:
  selector:
    app.kubernetes.io/name: {{ include "elt-platform.fullname" . }}
    app.kubernetes.io/component: frontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
      name: http
