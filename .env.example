# DT Orch SDK configuration (reference for customer apps)
# Platform install env template: see .env.platform.example

ELT_API_URL=https://api.example.com
# Bearer token is supplied at runtime via the SDK client constructor
