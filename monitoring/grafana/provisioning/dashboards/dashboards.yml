apiVersion: 1

providers:
  - name: DT Orch
    orgId: 1
    folder: DT Orch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
